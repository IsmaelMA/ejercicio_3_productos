CREATE TABLE
    `bd_ejercicio3`.`productos` (
        `codigoProducto` INT NOT NULL,
        `producto` VARCHAR(45) NULL,
        `precioUnitario` DOUBLE NULL,
        `stock` INT NULL,
        PRIMARY KEY (`codigoProducto`)
    );